<Window x:Class="TarkovHelper.Windows.Dialogs.CustomMarkerEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="커스텀 마커 편집"
        Width="350" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="White"
        FontFamily="Segoe UI, Malgun Gothic">

    <Window.Resources>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#B0B0B0"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

        <Style x:Key="ColorOption" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Width="30" Height="30" CornerRadius="15" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent" BorderThickness="2" Margin="4">
                            <Grid>
                                <Ellipse x:Name="check" Width="10" Height="10" Fill="White" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#2196F3"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#42A5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="커스텀 마커 편집" Style="{StaticResource HeaderStyle}"/>

        <!-- 이름 편집 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="이름" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="TxtName" Style="{StaticResource ModernTextBox}" MaxLength="50"/>
        </StackPanel>

        <!-- 색상 선택 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="색상" Style="{StaticResource LabelStyle}"/>
            <WrapPanel x:Name="ColorPanel">
                <RadioButton Background="#FF4444" Style="{StaticResource ColorOption}" Tag="#FF4444" IsChecked="True"/>
                <RadioButton Background="#4CAF50" Style="{StaticResource ColorOption}" Tag="#4CAF50"/>
                <RadioButton Background="#2196F3" Style="{StaticResource ColorOption}" Tag="#2196F3"/>
                <RadioButton Background="#FFEB3B" Style="{StaticResource ColorOption}" Tag="#FFEB3B"/>
                <RadioButton Background="#FF9800" Style="{StaticResource ColorOption}" Tag="#FF9800"/>
                <RadioButton Background="#9C27B0" Style="{StaticResource ColorOption}" Tag="#9C27B0"/>
                <RadioButton Background="#FFD700" Style="{StaticResource ColorOption}" Tag="#FFD700"/>
                <RadioButton Background="#FFFFFF" Style="{StaticResource ColorOption}" Tag="#FFFFFF"/>
            </WrapPanel>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <Grid Margin="0,0,0,6">
                <TextBlock Text="크기" Style="{StaticResource LabelStyle}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TxtSizeValue" Text="24" Foreground="#2196F3" HorizontalAlignment="Right" FontWeight="Bold"/>
            </Grid>
            <Slider x:Name="SliderSize" Minimum="12" Maximum="64" Value="24" TickFrequency="2" IsSnapToTickEnabled="True" 
                    ValueChanged="SliderSize_ValueChanged"/>
        </StackPanel>


        <!-- 하단 버튼 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="취소" Style="{StaticResource ActionButton}" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="저장" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
